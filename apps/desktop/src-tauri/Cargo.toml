[package]
name = "peercomms-desktop"
version = "0.1.0"
description = "PeerComms local desktop client"
authors = ["PeerComms contributors"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
